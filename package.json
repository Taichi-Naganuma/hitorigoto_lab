{
  "name": "hitorigoto-lab",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && npx --yes wrangler deploy",
    "quality-floor": "node scripts/quality-floor-check.mjs",
    "test:quality-floor": "node --test test/quality-floor.test.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/sitemap": "^3.2.0"
  }
}
